node_modules
dist
.DS_Store
.env

# Ignore the output video from Git but not videos you import into src/.
out

# Local agent tool config (per-machine, not shared)
.agent/
.agents/
.bob/
.claude/
.codex/
.gemini/
.opencode/
out/

# Music bed — fetched at setup time via npm run setup:music
public/music.mp3

# Renders go here — kept local, not committed (~50 MB MP4 + GIF).
# If a render should ship in the README, copy or rename it under
# /screenshots/ (parent repo root) — that folder is git-managed.
