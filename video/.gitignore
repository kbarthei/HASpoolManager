node_modules
dist
.DS_Store
.env

# Ignore the output video from Git but not videos you import into src/.
out

# Local agent tool config (per-machine, not shared)
.agent/
.agents/
.bob/
.claude/
.codex/
.gemini/
.opencode/
out/

# Music bed — fetched at setup time via npm run setup:music
public/music.mp3

# Screenshots are mirrored from ../screenshots/ at setup-time via
# npm run setup:screenshots — single source of truth lives at the
# HASpoolManager repo root, not duplicated into git here.
public/screenshots
public/screenshots/
