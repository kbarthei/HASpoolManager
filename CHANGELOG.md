# Changelog

All notable changes to HASpoolManager are documented here.
Generated automatically from [conventional commits](https://www.conventionalcommits.org/).


## April 2026

- **feat:** brand mark + deployed version in header, repo link on admin (`03c9836`)
- **docs:** regenerate CHANGELOG.md [skip ci] (`5b78a1f`)
- **test:** fix selector ambiguity in 3 specs (`0417e4e`)
- **docs:** regenerate CHANGELOG.md [skip ci] (`b335573`)
- **fix:** unblock CI — lint error + supply-engine test dates + new e2e coverage (`3bab05d`)
- **docs:** regenerate CHANGELOG.md [skip ci] (`92c178f`)
- **feat:** 2-column /orders layout + move Supply & Budget from /admin (`9804be3`)
- **docs:** regenerate CHANGELOG.md [skip ci] (`99baffb`)
- **feat:** fold health-check findings into Diagnostics dashboard (`07b8eeb`)
- **docs:** regenerate CHANGELOG.md [skip ci] (`33b7585`)
- **feat:** SQL execute endpoint for admin-driven data repairs (`50a8e59`)
- **feat:** diagnostics dashboard with issue-card deep-linking (`4de81ac`)
- **docs:** regenerate CHANGELOG.md [skip ci] (`5edb043`)
- **chore:** bump addon version to 1.0.84 (`1799bc8`)
- **docs:** regenerate CHANGELOG.md [skip ci] (`51dbd6e`)
- **feat:** 2-column layout for /admin on desktop (`e1958a6`)
- **docs:** regenerate CHANGELOG.md [skip ci] (`c7c9f5a`)
- **chore:** bump addon version to 1.0.83 (`2e6c1f5`)
- **docs:** regenerate CHANGELOG.md [skip ci] (`6a54dc5`)
- **feat:** /spools uses the Inspector panel; widen /admin + /history on desktop (`2557e4f`)
- **docs:** regenerate CHANGELOG.md [skip ci] (`2746bac`)
- **chore:** bump addon version to 1.0.82 (`ebd3b78`)
- **docs:** regenerate CHANGELOG.md [skip ci] (`8b27f20`)
- **feat:** apply redesign system to /admin (`f99fc2e`)
- **feat:** apply redesign system to /history (`c3841be`)
- **feat:** apply redesign system to /spools list + SpoolCard (`8b012f7`)
- **feat:** /spools/[id] full-page rebuild using the Inspector system (`83c020e`)
- **feat:** apply redesign system to /orders (`ff3b8ce`)
- **feat:** apply redesign system to /prints (`cc2b8b5`)
- **fix:** extend filter dimming to AMS + AMS HT + External chips (`03aa3f4`)
- **docs:** regenerate CHANGELOG.md [skip ci] (`e91dc3f`)
- **chore:** bump addon version to 1.0.81 (`e8693d1`)
- **docs:** regenerate CHANGELOG.md [skip ci] (`984baf7`)
- **chore:** delete dead /ams + /storage routes, unify on Spool Inspector (`eb31378`)
- **feat:** 3.3 Spool Inspector — slide-in panel on desktop, bottom sheet on mobile (`8d373c7`)
- **feat:** 3.2 Inventory redo — new rack, AMS strip, filter chips (`7dc3ebc`)
- **feat:** redesign foundation — tokens, Analytics route, Dashboard rebuild (`9e1eacb`)
- **docs:** regenerate CHANGELOG.md [skip ci] (`05a5ae8`)
- **chore:** bump addon version to 1.0.80 (`dfece84`)
- **chore:** ignore /handoff reference package from build + git (`8548c19`)
- **fix:** always run initial sync on startup, not only when active (`d128a89`)
- **docs:** regenerate CHANGELOG.md [skip ci] (`29bdfd6`)
- **chore:** bump addon version to 1.0.78 (`9442f2d`)
- **fix:** don't unbind AMS slots when HA entities go unavailable (`1e68f99`)
- **docs:** regenerate CHANGELOG.md [skip ci] (`94b25d0`)
- - refactor: move Clear Stale to Prints page, Refresh Prices to Shop Configuration (`42d1962`)
- **docs:** regenerate CHANGELOG.md [skip ci] (`dd59309`)
- - refactor: group admin page into 5 zones by adjustment frequency (`a370eb6`)
- **docs:** regenerate CHANGELOG.md [skip ci] (`265391a`)
- **chore:** bump addon version to 1.0.75 (`639e71d`)
- **feat:** Supply Manager Phase 2 — budget tracking, shop config, order optimizer (`46456c5`)
- **docs:** regenerate CHANGELOG.md [skip ci] (`0b5aa5e`)
- - refactor: use perceptual color distance (ΔE) for AMS swap detection (`dd67b86`)
- **fix:** skip fuzzy matching after swap when there's no RFID (`26dc6c5`)
- **docs:** regenerate CHANGELOG.md [skip ci] (`72001b8`)
- **fix:** compare swap against linked spool's filament color, not slot's bambu_color (`3cf7aad`)
- **docs:** regenerate CHANGELOG.md [skip ci] (`05fa098`)
- **fix:** detect AMS filament swap and unbind stale draft spool (`8c6586c`)
- **docs:** regenerate CHANGELOG.md [skip ci] (`cf80e3b`)
- **feat:** admin Data Quality card + unified cleanup script (`72a7e30`)
- **docs:** regenerate CHANGELOG.md [skip ci] (`b445d3a`)
- **chore:** bump addon version to 1.0.67 (`b2ef173`)
- **docs:** regenerate CHANGELOG.md [skip ci] (`be45cb6`)
- **docs:** bump test counts (490 unit / 90 integration) (`28592a0`)
- **feat:** data-quality backbone — triggers, health-check, quality log table (`2833a30`)
- **feat:** vendor aliases + name normalization for entity writes (`bd80fe9`)
- **docs:** regenerate CHANGELOG.md [skip ci] (`0f834ec`)
- **chore:** housekeeping — audit fix (hono), test counts, ignore iCloud dupes (`aef014f`)
- **docs:** regenerate CHANGELOG.md [skip ci] (`ac250b3`)
- **fix:** rename shadowed 'module' variable to pass Next.js lint (`2ce962f`)
- **docs:** regenerate CHANGELOG.md [skip ci] (`a9b3358`)
- **chore:** bump addon version to 1.0.66 (`c9d40df`)
- **feat:** backfill historical energy costs with per-material model (`b399aa2`)
- - refactor: move energy settings save to Server Action (`3fca08f`)
- **chore:** add shop deduplication script (`aeaa816`)
- **fix:** fuzzy shop matching to prevent duplicate shop entries (`b21dba4`)
- **feat:** add-to-shopping-list button on supply alerts (`be99d81`)
- **feat:** add-to-shopping-list button on supply views (`59516cf`)
- **feat:** add /supply page with full filament supply status list (`36d1986`)
- **feat:** Supply Manager Phase 1 — consumption tracking, supply alerts, material profiles (`5e75438`)
- **fix:** skip bare material names in color lookup + add correction preview script (`58edc09`)
- **feat:** vendor color lookup + default spool tare weights (`3596c19`)
- **feat:** electricity cost tracking + HMS error correlation (`a2bce51`)
- **docs:** regenerate CHANGELOG.md [skip ci] (`26815b9`)
- **chore:** add workdir/ for local working files (plans, notes, temp) (`0771b9e`)
- **docs:** regenerate CHANGELOG.md [skip ci] (`82395f1`)
- **chore:** remove implementation plans from git, add to .gitignore (`f314ae6`)
- **docs:** regenerate CHANGELOG.md [skip ci] (`4948b45`)
- **fix:** restore changelog push-to-main (branch protection now allows it) (`9263b09`)
- **fix:** changelog workflow triggers on tags only (no push to main) (`178a7ea`)
- **chore:** add MIT license (`9373372`)
- **docs:** regenerate CHANGELOG.md [skip ci] (`e932267`)
- **fix:** cover image fetch — strip entity token, use supervisor auth (`9c22877`)
- **docs:** regenerate CHANGELOG.md [skip ci] (`173a424`)
- **fix:** map camera entity (Kamera) for snapshot capture (`e1556bc`)
- **docs:** regenerate CHANGELOG.md [skip ci] (`114b716`)
- **test:** integration test for print image paths (PM-8) (`d79c451`)
- **feat:** display cover image + snapshot on prints page and hero card (PM-7) (`7c55993`)
- **feat:** API endpoints for print image storage + serving (PM-5, PM-6) (`faaa492`)
- **feat:** capture cover image at print start + snapshot at finish (PM-4) (`7fff282`)
- **feat:** map cover image entity (Titelbild) in HA discovery (PM-3) (`e292d13`)
- **feat:** add cover_image_path + snapshot_path columns to prints (PM-2) (`f08719a`)
- **feat:** add callHAService to HA API client (PM-1) (`0979c13`)
- **docs:** regenerate CHANGELOG.md [skip ci] (`866c723`)
- **fix:** add data-testid on spool detail + aria-label on sync refresh (audit #11, #12) (`5c2e720`)
- **docs:** regenerate CHANGELOG.md [skip ci] (`d00a8ba`)
- **fix:** GET endpoints use optionalAuth for browser access (audit #8) (`55b5e1b`)
- **fix:** order cards keyboard accessible with role=button (audit #7) (`1158677`)
- **fix:** add loading.tsx spinner for all async pages (audit #6) (`f9b9721`)
- **fix:** sanitize error responses in printer-mappings (audit #5) (`44ee221`)
- **fix:** use bool() helper for HA values in ams-slot-changed (audit #4) (`271cd5d`)
- **fix:** explicit field destructuring in PUT routes, no ...body spread (audit #3) (`330ae5c`)
- **fix:** remove broken hsl() wrapper from chart stroke colors (audit #2) (`43bd077`)
- **docs:** regenerate CHANGELOG.md [skip ci] (`a3266e9`)
- **docs:** add generic best practices to CLAUDE.md coding standards (`fa1cd5f`)
- **docs:** regenerate CHANGELOG.md [skip ci] (`4acf415`)
- **docs:** comprehensive coding standards in CLAUDE.md (lessons learned) (`e7a4e26`)
- **docs:** regenerate CHANGELOG.md [skip ci] (`c6991d3`)
- **fix:** analytics toggle — Space key + aria-expanded (M5 review fix) (`93c5393`)
- **fix:** prefers-reduced-motion fallback for animations (M4 review fix) (`20f5477`)
- **fix:** add data-testid to new components per CLAUDE.md (M3 review fix) (`fb4cbdf`)
- **test:** document dashboard path convention in mobile-viewport spec (M2 review fix) (`c40ecb6`)
- **docs:** sync test-strategy.md with 3 new e2e specs (M1 review fix) (`c5bddc9`)
- **docs:** regenerate CHANGELOG.md [skip ci] (`dfda1f2`)
- - design: analytics toggle — full card button instead of thin text (`ffef330`)
- **docs:** regenerate CHANGELOG.md [skip ci] (`f9a015e`)
- - design: collapsible analytics section on dashboard (`889d0ed`)
- - design: card entry animations — fade-in-up with stagger (`483515f`)
- - design: spool detail — color accent header with gradient tint (`987a833`)
- - design: mobile nav — larger touch targets (h-14→h-16, icons 5→6) (`bac9538`)
- - design: currently printing hero card with progress ring (`da82c25`)
- - design: spool cards — low-stock amber ring + badge, empty opacity (`11672da`)
- - design: fix chart colors — replace broken hsl(var(--primary)) with hex (`cfbb7c9`)
- - design: stat cards — larger numbers (3xl→4xl) + teal accent on primary (`9d4e68d`)
- **docs:** regenerate CHANGELOG.md [skip ci] (`45184bd`)
- **fix:** dashboard e2e path './' not 'ingress/' (same as smoke test) (`a990f6f`)
- **docs:** regenerate CHANGELOG.md [skip ci] (`4830411`)
- **fix:** mobile-viewport e2e test timeout for dashboard (slow render) (`3d51d25`)
- **docs:** regenerate CHANGELOG.md [skip ci] (`6b415b3`)
- **test:** add 3 nice-to-have e2e specs (dark-mode, scan-flow, mobile-viewport) (`50b497b`)
- **docs:** regenerate CHANGELOG.md [skip ci] (`516ce83`)
- **fix:** Docker CI — use alpine:3.21 base + QEMU for cross-arch builds (`0f4d355`)
- **docs:** regenerate CHANGELOG.md [skip ci] (`bcbfc12`)
- **fix:** replace require('fs') with import in next.config.ts (lint error) (`f9b398b`)
- **docs:** regenerate CHANGELOG.md [skip ci] (`5dbe39d`)
- **chore:** cleanup stale files, env vars, and references (`6b74420`)
- **docs:** regenerate CHANGELOG.md [skip ci] (`e93d3be`)
- **feat:** show addon version on admin page (Build & Cache section) (`0da2b6e`)
- **docs:** regenerate CHANGELOG.md [skip ci] (`fa2fd19`)
- **fix:** e2e hydration test filters for chunk load + admin API errors (`5ed6e7f`)
- **docs:** regenerate CHANGELOG.md [skip ci] (`b0ab60e`)
- **fix:** graceful fetch errors in client components (fixes e2e hydration) (`bf37af8`)
- **docs:** regenerate CHANGELOG.md [skip ci] (`81e8fda`)
- **fix:** CI failures — empty public dir + Dockerfile BUILD_FROM arg scoping (`c08415c`)
- **docs:** regenerate CHANGELOG.md [skip ci] (`f0eb04f`)
- **fix:** lint errors + add spool_swaps DB migration for test harness (`186ec1a`)
- **docs:** regenerate CHANGELOG.md [skip ci] (`03d7b1c`)
- - security: fix CRITICAL + HIGH findings from security audit (`dcc11f6`)
- **docs:** regenerate CHANGELOG.md [skip ci] (`708d0dc`)
- - ci: add security workflow (dependency audit + static analysis) (`658b158`)
- **docs:** regenerate CHANGELOG.md [skip ci] (`778d859`)
- **chore:** remove archived docs (historical planning, no longer needed) (`fbf0377`)
- **docs:** regenerate CHANGELOG.md [skip ci] (`f3bf39e`)
- **chore:** remove all Spoolman references + fix Vercel URLs (`7c74b39`)
- **docs:** regenerate CHANGELOG.md [skip ci] (`0d57c43`)
- **feat:** Docker CI pipeline + addon repo for HA installation (`7fc2922`)
- **docs:** regenerate CHANGELOG.md [skip ci] (`ca7e64b`)
- **docs:** full documentation refresh with screenshots (v1.0.53) (`cb4f1f1`)
- **docs:** regenerate CHANGELOG.md [skip ci] (`b32118d`)
- **feat:** auto-migrate DB schema on addon start (`10e1bdc`)
- **docs:** regenerate CHANGELOG.md [skip ci] (`71bffa6`)
- **fix:** swap detection via bambu_lab_event instead of binary_sensor state (`65453cc`)
- **docs:** regenerate CHANGELOG.md [skip ci] (`c26e73d`)
- **fix:** sync worker reads initial printer state after restart (`1dfa245`)
- **docs:** regenerate CHANGELOG.md [skip ci] (`d70055b`)
- **feat:** entity mapping overrides with auto/manual indicator + reset (`b2cb7f2`)
- **docs:** regenerate CHANGELOG.md [skip ci] (`4a9a1ba`)
- **feat:** entity mapping override in admin UI + read-only DB query endpoint (`56b9398`)
- **docs:** regenerate CHANGELOG.md [skip ci] (`38f3d01`)
- **feat:** AMS drying status indicator on inventory page (`01ccc6a`)
- **docs:** regenerate CHANGELOG.md [skip ci] (`5d7ddae`)
- **chore:** UI polish — tab order, font sizes, admin text (`2738c65`)
- **docs:** regenerate CHANGELOG.md [skip ci] (`027227f`)
- **fix:** handle instant-cancel (FAILED without prior STARTED event) (`80cd825`)
- **docs:** regenerate CHANGELOG.md [skip ci] (`8e2bec5`)
- **feat:** mid-print spool swap detection (Phase 6) (`2b81de0`)
- **docs:** regenerate CHANGELOG.md [skip ci] (`e0274c8`)
- **fix:** changelog workflow needs contents:write permission to push (`cf4884b`)
- **feat:** per-tray weight from 3MF attributes (Phase 7) (`b62c21a`)
- **fix:** admin printer mappings fetch path for ingress/PWA compatibility (`ec0ddaa`)
- **fix:** load SUPERVISOR_TOKEN before Next.js + use optionalAuth for admin mappings (`f1c8794`)
- **feat:** admin UI for printer entity mappings (Phase 3b) (`838d0fc`)
- **docs:** update for sync worker migration (Phase 8 complete) (`a14d545`)
- **fix:** throttle sync worker events + improve logging (`a241cd6`)
- **fix:** sync worker entity discovery + printer matching + token loading (`6d94921`)
- **feat:** native HA integration — sync worker with websocket + auto-discovery (`4573be5`)
- **fix:** 3 sync bugs + 10 critical tests + state machine docs (`2e89fa1`)
- **docs:** comprehensive addon DOCS.md + GitHub Releases in deploy.sh (`6a59302`)
- **chore:** remove Sentry, boilerplate SVGs, and archived scripts (`a43d857`)
- - ci: auto-generate CHANGELOG.md on every push to main (`5fe3b39`)
- **feat:** spool link-to-order and merge-duplicate actions (`16cbe82`)
- **docs:** complete documentation overhaul (`4e11c97`)
- **fix:** scale failed print usage by progress + add direct PWA port + fix locale (`824c1f9`)
- **docs:** add test-strategy sync rule to CLAUDE.md (`819e89d`)
- **docs:** sync test-strategy.md with actual implementation (`d800daf`)
- **feat:** add script to link unlinked order items to spools (`92d2ffd`)
- **chore:** upgrade TypeScript 6.0.2 + merge Dependabot minor/patch batch (`6b4e060`)
- **chore:** bump @types/node from 20.19.37 to 25.5.0 (#6) (`1a89481`)
- **chore:** bump @types/bcryptjs from 2.4.6 to 3.0.0 (#7) (`1456cbd`)
- **chore:** bump the minor-and-patch group across 1 directory with 15 updates (#11) (`89fead6`)
- **chore:** bump actions/setup-node from 4 to 6 (#2) (`82280d3`)
- **chore:** bump actions/checkout from 4 to 6 (#1) (`665f3f8`)
- **chore:** activate CI e2e stage + update all docs (`11fda4f`)
- **test:** complete e2e spec suite, 25/25 green (`d6113c2`)
- **chore:** add page-<name> testids + update test-strategy.md to match code (`d15fe7f`)
- **test:** e2e harness — addon stack + ingress simulator + smoke spec (`7b7d58c`)
- **test:** refuse to run if test DB path leaves tests/tmp/ (`62a17de`)
- **chore:** bump addon to 1.0.22 (`c7135cc`)
- **chore:** scrub Vercel/Neon/Postgres legacy from admin page, docs, and CI (`fc65c79`)
- **chore:** bump addon to 1.0.21 (`fba3173`)
- **fix:** classify CANCELED/CANCELLED gcode_state as failed (`1f4669a`)
- **test:** migrate all integration tests to harness, 59/59 green (`458b50e`)
- **chore:** gitignore testdata/ dir (`9ea5ee2`)
- **chore:** untrack private testdata/ and add to .gitignore (`75f7a0d`)
- **test:** add per-worker SQLite harness + POC integration test (`6aed419`)
- **chore:** bump addon to 1.0.20 (`4c10dcc`)
- **fix:** use sm:max-w-5xl so dialog actually widens past primitive cap (`cf6f2dc`)
- **chore:** bump addon to 1.0.19 (`bf79b39`)
- **fix:** widen CSV import dialog so all columns are visible (`72501f9`)
- **chore:** bump addon to 1.0.18 (`4017f07`)
- **feat:** CSV order import review mode with per-item match editing (`263fd6c`)
- **chore:** bump addon to 1.0.17 (`571c054`)
- **feat:** add 6 dashboard charts (duration, success rate, waste, colors, vendor quality, stock value) (`4e0b74a`)
- **feat:** consolidate SQLite migration, drop Postgres/Vercel artifacts (`77bed08`)
- **feat:** Phase 10 — HA ingress working end-to-end with printer-sync (`89229d5`)
- **feat:** Phase 9 — Docker build working, add .dockerignore (`fa8cfe7`)
- **fix:** Phase 8 — SQLite compat fixes from local testing (`1ae30e6`)
- **docs:** HA migration status — Phase 1-7 done, Phase 8 (local SQLite test) next (`dde9aef`)
- **feat:** Phase 7 — Postgres to SQLite migration script (`0e79479`)
- **feat:** Phase 5 — HA ingress proxy (proxy.ts for Next.js 16) (`3dde5fc`)
- **feat:** HA addon migration phases 1-4 — SQLite schema, dual-driver, SQL compat, addon structure (`b4442b7`)
- **fix:** spool history uses print date (not record creation date) + proper filament names (`981fac8`)
- **fix:** autoCreateDraftSpool checks existing slot assignment — prevents 100+ duplicate drafts (`b017541`)
- **feat:** proportional weight distribution + manual usage weight adjustment (`35ee766`)
- **feat:** use gcode_state for print lifecycle — 10 stable states instead of 68+ fragile ones (`becf4a5`)
- **fix:** auto-calibration routines no longer create print records (`3f29c90`)
- **fix:** AUTO_BED_LEVELING + OFFLINE no longer kill running prints (`9e85279`)
- **feat:** interactive sync log — refresh, pagination, filters, auto-refresh (`f7aa3fc`)
- **feat:** 4 more dashboard charts — spend by vendor, filament consumed, spool lifecycle, material usage (`9673d40`)
- **feat:** dashboard charts — monthly spend, inventory by material, prints per month (`a702ecc`)
- **fix:** sync_log cleanup was deleting ALL real HA logs — now scoped to test run time only (`1e169f5`)
- **fix:** extend sync log retention from 24h to 72h, use Drizzle lt() for deletion (`5ffb880`)
- **fix:** active spool matching falls back to fuzzy when RFID tag is zeros (`d972eaa`)

## March 2026

- **test:** CI — add CHANGING_FILAMENT unit test + fix E3 stale-data false-positive (`c3ef2db`)
- **fix:** add CHANGING_FILAMENT to active states — was misclassified as idle, splitting multi-filament prints (`f024251`)
- **feat:** spool weight sync from AMS remain percentage + 23 tests (`fe471b6`)
- **feat:** multi-filament print tracking — accumulate all spools used during a print (`a15ce3c`)
- **feat:** Add to Shopping List button on both spool detail views (`97e60d9`)
- **feat:** price refresh buttons + fix PLA Silk+ URL (`3667116`)
- **feat:** dashboard shows prints count + admin config details (HA, printer, DB, AI) (`7c79e36`)
- **fix:** removed spool goes to surplus (not storage) — storage location does not exist (`883731d`)
- **test:** Phase 5 — 124 new unit tests for validations, date formatters, matching edge cases (`bbaa874`)
- **fix:** CI — move ejected spool to storage when slot becomes empty (`14d1737`)
- **fix:** CI cleanup step runs always (even on test failure) to delete test data from prod DB (`852d298`)
- **fix:** move parseRackPosition to lib/rack-helpers.ts (can't be in 'use server' file) (`304632e`)
- - refactor: Phase 4 — conceptual tests now import real production code (251 tests) (`b395a31`)
- **fix:** test cleanup matches all test print patterns (test-*, Integration Test Print, sync_*_test-*) (`fb25228`)
- **fix:** rack cells show material badge like workbench cards (`d7cb70d`)
- **fix:** eliminate 'storage' limbo — auto-move to workbench, delete bogus TPU spool (`2e747f1`)
- **fix:** unified timezone — all dates use Europe/Berlin via shared lib/date.ts (`4504fd7`)
- **fix:** findings #9-11 — stale test cleanup, drop audit_log, verify order filaments (`cb883b0`)
- **fix:** CI — use getByRole in e2e tests to avoid strict mode violations (`50557e3`)
- **fix:** add data-testid to orders page Add Order button (`e34f4b9`)
- **fix:** CI type error — await findFirst before null coercion (`c654323`)
- **feat:** sync spool detail sheet with full page — add used weight, cost/g, RFID, usage history (`80a1d46`)
- **fix:** spool detail page — order section is now clickable, links to /orders (`41e6b74`)
- **feat:** admin — build info (commit, deploy time, region) + cache purge button (`ca5997a`)
- **fix:** integration tests clean up test prints, delete leftover test data from DB (`e956d83`)
- **feat:** bidirectional spool ↔ order navigation (`a9e9856`)
- **feat:** CSV bulk order import — upload Excel export, auto-match spools, batch import (`d6b9215`)
- **feat:** historical order import — paste email, AI parse, match to existing spools (`99f24d6`)
- - ci: restructure pipeline — smart triggering + testing convention in CLAUDE.md (`1912a29`)
- - refactor: Phase 3 — e2e stabilization with data-testid attributes (`e127e2d`)
- **fix:** integration tests — add auth headers to vendors/match endpoints, relax spool count (`d51d632`)
- **test:** Phase 2 — 27 integration tests for printer-sync endpoint (`10e491d`)
- - refactor: extract printer-sync helpers + 92 unit tests (Phase 1) (`6b3bf48`)
- **docs:** comprehensive test strategy + templates for evolving test suite (`5afa768`)
- **fix:** CI tests — update smoke test for /inventory, fix e2e selectors for merged tabs (`e2ffae0`)
- **fix:** rack cells horizontal layout (color left, text right) + bump dashboard font sizes (`92848df`)
- **fix:** operator precedence in bambuFilamentName (`0127139`)
- **fix:** normalize filament names — 34 filaments cleaned, naming convention enforced (`2b701b0`)
- **fix:** sync log retention (24h), revalidate /inventory, delete fake orders + stale spools (`d193e5a`)
- **feat:** merge AMS + Storage into single Inventory tab (`ada7dee`)
- - refactor: move Clear Rack button to Admin tab rack settings (`87c0f37`)
- **fix:** View Details on rack cells opens spool detail sheet (`7081aea`)
- **fix:** rack cells show vendor name + material + weight (`459b1d6`)
- **fix:** use correct Anthropic model ID (claude-sonnet-4-6 with hyphens) (`cfe23a6`)
- **fix:** scan endpoint — return error details for debugging (`2795982`)
- **fix:** scan endpoint — proper base64 image handling for AI SDK vision (`d6b7115`)
- **feat:** Add Spool — 3 methods: Scan Label (AI Vision), Filament Library, Clone (`4ee2aa0`)
- **feat:** auto-move out-of-bounds spools to workbench + Clear Rack button (`57095d6`)
- **feat:** customizable spool rack — configurable rows/columns, R1 at bottom (`2a706da`)
- **fix:** store activeSpoolId on print while printing, use it for usage on finish (`10b78df`)
- **fix:** revalidate cached pages on print state transitions and slot updates (`5899c73`)
- **feat:** auto-create Bambu spools from RFID + draft workflow for unknown spools (`afb9d47`)
- **fix:** show filament color name in prints, fix PLA Basic White spool + RFID mapping (`fbefb13`)
- **feat:** show active spool on dashboard + prints page during printing (`90a60d7`)
- **fix:** printer-sync — unique event IDs for repeated prints, handle calibration states (`62c9279`)
- **feat:** print usage tracking — link prints to spools, deduct weight, calculate cost on finish (`e1b04c3`)
- **fix:** handle undefined progress/remainingTime on dashboard (`72d7a8f`)
- **feat:** dashboard shows print progress % and remaining time when printing (`3c99aed`)
- **fix:** sync log detail panel — wrap sibling rows in Fragment (`3ab084b`)
- **fix:** admin sync log — show JSON detail panel on row click (`2282cdf`)
- **feat:** admin sync log — click to expand, stores full request+response payload (`e5bca05`)
- **feat:** admin tab with sync log, system stats, and stale print cleanup (`7156cfc`)
- **feat:** printer-sync v2 — flat payload, tolerant parsing, server-side normalization (`e753139`)
- **fix:** printer-sync accepts HA sensor values (PRINTING/FINISHED/CANCELED) not just MQTT values (`675d020`)
- **fix:** e2e test — AMS Status renamed to AMS Slots (`2cb2b97`)
- **feat:** dashboard AMS shows all 6 slots grouped by type with material badges (`a15849c`)
- **fix:** dashboard AMS mini view — exclude external slot, show filament name (`6c67688`)
- **fix:** don't finish print when IDLE + filament error (spool swap in progress) (`35bf7a1`)
- **feat:** printer-sync endpoint with filament error tracking + active slot (`683578a`)
- **feat:** printer-sync endpoint — single call syncs print state + all AMS slots (`281fc8f`)
- **feat:** prints page shows currently running prints with live indicator (`651d4a6`)
- **fix:** printers API loads full spool+filament+vendor relations (`1c5f3b3`)
- **fix:** friendly error page for DB cold starts + Neon wake-up (`080d4b5`)
- **fix:** e2e tests — use specific selectors, fix renamed labels (`a806f4a`)
- - ci: trigger e2e test with fixed DATABASE_URL secret (`f15602f`)
- **fix:** CI e2e — pass secrets directly to test step (`cb0a1eb`)
- **fix:** CI e2e — add API_SECRET_KEY env, fix DATABASE_URL secret (`32439cc`)
- **fix:** CI e2e tests — remove invalid --project flag, set chromium default (`7fa87a6`)
- **feat:** AMS slots show material badge + weight in grams (`95a5cd0`)
- **docs:** user stories — procurement, printing, spool management workflows (`43eaea6`)
- **docs:** getting started, deployment, configuration guides + CONTRIBUTING (`5351643`)
- **docs:** professional README with features, architecture, quick start (`b9006e7`)
- **docs:** architecture overview, data model ER diagram, API reference (`e50b3d2`)
- **docs:** user stories — procurement, printing, spool management workflows (`4364a4d`)
- **docs:** professional README with architecture diagram, features, quick start (`4dceed7`)
- **chore:** project cleanup — remove obsolete files, archive old docs (`0c488e2`)
- - ci: add Playwright e2e tests + test report upload on failure (`927eadd`)
- - security: add Zod input validation to all POST API routes (`f50e95b`)
- - ops: add Sentry error monitoring (disabled until DSN configured) (`b675b7b`)
- **chore:** Dependabot config, npm audit in CI, minor fixes (`ab3ba82`)
- - security: add HTTP security headers (HSTS, X-Frame, nosniff, referrer) (`35291ce`)
- **chore:** generate baseline DB migration, document migration workflow (`72928c8`)
- **feat:** archive/restore/delete spools — full lifecycle (`b656db0`)
- **feat:** deploy to Frankfurt region (fra1) for EUR pricing (`ad0dc49`)
- **fix:** show currency on shop prices, skip EUR/USD comparison (`762839e`)
- **fix:** extract EUR price from Bambu Lab EU store HTML body (`9780ec2`)
- **fix:** use German locale headers for EUR pricing in price crawler (`b6b522d`)
- **fix:** create shop listings from orders, fix Bambu ProductGroup price parsing (`817ec8c`)
- **test:** price crawler parsing, price comparison, shopping list calculations (`b20afc8`)
- **feat:** shopping list with price history + current price comparison (`0aad89e`)
- **feat:** price crawler with shop-specific parsers + refresh API (`3b702df`)
- **feat:** shopping list schema, actions, price history queries (`7caa85f`)
- **feat:** move spools from surplus/workbench back to rack with slot picker (`8aa5ab1`)
- **fix:** use onClick instead of onSelect for base-ui DropdownMenu items (`fbc7a3c`)
- **fix:** move bash permissions to project settings — subagents inherit them (`82bdde3`)
- **test:** storage moves, weight adjustment, order detail tests (`32eb777`)
- **feat:** spool weight adjustment + storage moves with workbench (`5311401`)
- **feat:** order detail sheet — click any order to see full details (`6693124`)
- **fix:** revalidate /orders path after receiving order (`a30f74d`)
- **fix:** receive wizard — link spools to orders by filament_id, cleaned duplicates (`5f565ff`)
- **test:** comprehensive order handling tests — parsing, filtering, grouping, e2e (`b7a06ab`)
- **feat:** orders page split view — pending pinned, month groups, filters (`a9776bc`)
- **fix:** AI always returns English color names, fixed existing German names in DB (`ffb83c1`)
- **fix:** order creation — handle null fields, prevent duplicate filaments (`e875441`)
- **test:** add Orders page to smoke test (`e1c93d3`)
- **feat:** Orders page + surplus storage + navigation update (`497ce06`)
- **feat:** order receive wizard with guided rack placement (`0ee1f42`)
- **feat:** Add Order dialog with AI parsing and review (`8f3618c`)
- **feat:** AI-powered order parsing API + order creation actions (`04fd1c6`)
- **docs:** smart spool adding spec + plan (`95a7754`)
- **fix:** CI smoke test — use production URL instead of preview wait (`3c72e3c`)
- **fix:** CI — lazy DB init, type check instead of build, fix test types (`6ff524e`)
- **fix:** add PreToolUse hook to block cd prefix in all agents (`0de850d`)
- **fix:** resolve all CI lint errors — 0 errors, 0 warnings (`59f0851`)
- **feat:** printer selector for multi-printer support (`cdc8140`)
- **feat:** clickable prints, filament summary card, dashboard layout (`4b39660`)
- **feat:** spool detail shows purchase shop link from order data (`85dc23a`)
- **feat:** clickable AMS slots in dashboard — link to spool detail (`af76f6b`)
- **feat:** labeled spool filters + color filter with color dots (`c96faa5`)
- **docs:** iOS Shortcut setup guide for NFC spool scanning (`520d164`)
- **feat:** NFC scan route — /scan?tag=UID redirects to spool detail (`d40ac70`)
- **test:** add print history + spool history to smoke test (`cdf8b63`)
- **feat:** print history + spool history pages, updated navigation (`628a31c`)
- **feat:** storage grid — larger cells, drag and drop, better readability (`b778726`)
- **feat:** clickable dashboard stats and low stock links (`e0356dc`)
- - ci: GitHub Actions — lint, unit tests, build, smoke test (`e2750a5`)
- **fix:** use optionalAuth for tags GET routes (`e29d6c6`)
- **fix:** remove invalid environmentMatchGlobs from vitest config (`3e67a03`)
- **test:** add smoke test script for post-deploy verification (`3215132`)
- **docs:** add no-cd-prefix rule to CLAUDE.md for all agents (`ff73b81`)
- **test:** e2e tests for navigation, dashboard, spools, AMS, storage, theme (`fbdb632`)
- **test:** unit tests for color, theme, and matching scoring (`05f23e6`)
- **test:** integration tests for API routes (health, match, events, CRUD) (`44d92b4`)
- **chore:** set up Vitest + Playwright test infrastructure (`49a8ad1`)
- **feat:** Phase 4 — frontend core pages, auth fix, toast notifications (`79fc883`)
- **feat:** storage rack page with configurable grid (`a188971`)
- **feat:** AMS status page with live refresh and spool picker (`5d7bce9`)
- **feat:** spool detail page and reusable detail sheet (`65c276f`)
- **feat:** spool inventory with grid/list toggle and filters (`3f9314e`)
- **feat:** dashboard page with stats, AMS mini, alerts, recent prints (`77f8748`)
- **feat:** shared spool components (color dot, progress, badge) (`2b343df`)
- **feat:** top tab bar + mobile bottom nav (`0cd87bd`)
- **feat:** install shadcn components and React Query (`79afc51`)
- **feat:** Apple system color theme with light/dark toggle (`59440d9`)
- **docs:** frontend core implementation plan (10 tasks, 32 files) (`b78cdbd`)
- **docs:** update frontend spec — Apple Health style, light+dark, teal accent (`7c5ade4`)
- **docs:** frontend core design spec (Phase 4) (`adc5e13`)
- **feat:** Phase 3 — event webhooks, slot_type for AMS/HT/external (`7966723`)
- **feat:** Phase 0-2 — schema, CRUD APIs, matching engine, seed data (`97cc6da`)
- **feat:** initial commit (`d5cc2f5`)
- **docs:** add CLAUDE.md, HA setup context, and migration reference (`7b5ea87`)
- **docs:** comprehensive project plan and architecture specs (`80bb0a5`)
- - Initial commit (`6cb13ee`)
